# Backend environment example for local development

# Flask server port
PORT=3001

# Comma-separated list of allowed origins for CORS
CORS_ORIGINS=http://localhost:3000

# Optional: Flask settings (if applicable in your app)
# FLASK_ENV=development
# FLASK_DEBUG=1
